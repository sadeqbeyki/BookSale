@model List<AppQuery.Contracts.Product.ProductQueryModel>

<div class="">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="">
                    <h2 class="">آخرین محصولات</h2>
                    <p class="">

                        آخرین محصولات
                        آخرین محصولات
                        آخرین محصولات
                        آخرین محصولات

                    </p>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="">


                    @foreach (var product in Model)
                    {
                        <div class="col">
                            <div class="">
                                <div class="">
                                    <div class="">
                                        @if (product.HasDiscount)
                                        {
                                            <span class="sale">@product.DiscountRate%</span>
                                            <span class="new">جدید</span>
                                        }
                                    </div>
                                    <a asp-page="/Product" asp-route-id="@product.Slug">
                                        <img src="~/ProductPictures/@product.Picture" class="img-fluid" title="@product.PictureTitle" alt="@product.PictureAlt">
                                    </a>

                                </div>
                                <div>
                                    <div>
                                        <span class="category"><a asp-page="/ProductCategory" asp-route-id="@product.CategorySlug">@product.Category</a></span>
                                        <span class="rating">
                                            <i class="ion-android-star active"></i>
                                            <i class="ion-android-star active"></i>
                                            <i class="ion-android-star active"></i>
                                            <i class="ion-android-star active"></i>
                                            <i class="ion-android-star-outline"></i>
                                        </span>
                                    </div>

                                    <h3>
                                        <a asp-page="/Product" asp-route-id="@product.Slug">@product.Name</a>
                                    </h3>

                                    <p>
                                        @if (product.HasDiscount)
                                        {
                                            <span class="discounted-price">@product.PriceWithDiscount تومان</span>
                                            <span class="main-price discounted">@product.Price تومان</span>
                                        }
                                        else
                                        {
                                            <span class="main-price">@product.Price تومان</span>
                                        }
                                    </p>

                                </div>
                            </div>
                        </div>
                    }

                </div>
            </div>
        </div>
    </div>
</div>
    </div>
